-- scripts/init.sql
-- This script can be used for initial database setup before the schema is applied.
-- For example, creating specific roles or extensions if needed.
-- For GestCyber, the main database 'gestcyber' and user 'gestcyber_user'
-- are created by environment variables in docker-compose.yml (POSTGRES_DB, POSTGRES_USER).
-- So, this file might remain empty or be used for more advanced setup later.

-- Example: CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
